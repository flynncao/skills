Use this trailer for commits created by Codex:

```text
Co-authored-by: Codex <codex@openai.com>
```

Append it to the message generated by the active commit workflow after one blank line. Preserve the original subject and body. Do not add the trailer when the message already contains a `Co-authored-by` trailer with the email `codex@openai.com`, compared case-insensitively.
