Use this trailer for commits explicitly attributed to Claude Code:

```text
Co-authored-by: Claude <claude@anthropic.com>
```

Append it to the message generated by the active commit workflow after one blank line. Preserve the original subject and body. Do not add the trailer when the message already contains a `Co-authored-by` trailer with the email `claude@anthropic.com`, compared case-insensitively.
